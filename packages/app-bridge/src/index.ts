/**
 * @last-mile-context/app-bridge
 *
 * Phase 3 で実装する。`window.__AI_DEBUG_CONTEXT__` への安全な公開層。
 */
export const __packageMeta = {
  name: '@last-mile-context/app-bridge',
  phase: 3,
  status: 'scaffold',
} as const;
